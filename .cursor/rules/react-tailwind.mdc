---
description: React component and Tailwind patterns for this project
globs: src/**/*.tsx
alwaysApply: false
---

# React + Tailwind Patterns

## Component template
```tsx
import type { FC } from 'react';
import { clsx } from 'clsx';

interface MyComponentProps {
  title: string;
  className?: string;
}

export const MyComponent: FC<MyComponentProps> = ({ title, className }) => (
  <div className={clsx('rounded-[12px] bg-wallet-surface-muted px-4 py-3', className)}>
    <p className="text-[15px] font-medium text-wallet-text">{title}</p>
  </div>
);
```

## Page template
```tsx
import type { FC } from 'react';
import { Page } from '@/components/Page.tsx';

export const MyPage: FC = () => (
  <Page>
    <div className="space-y-4 py-4">
      {/* content */}
    </div>
  </Page>
);
```

## Telegram-style section (list card)
```tsx
<section>
  <p className="px-4 pb-1.5 text-[13px] font-medium uppercase tracking-wide text-wallet-text-secondary">
    Section Title
  </p>
  <div className="mx-4 divide-y overflow-hidden rounded-xl border border-wallet-border bg-wallet-surface-muted">
    {/* rows */}
  </div>
  <p className="px-4 pt-2 text-[13px] text-wallet-text-muted">Footer note.</p>
</section>
```

## Navigation cell (tappable row)
```tsx
<Link
  to="/route"
  className="flex items-center gap-3 px-4 py-3 no-underline transition-colors hover:bg-white/5 active:bg-white/10"
>
  {/* optional icon */}
  <div className="min-w-0 flex-1">
    <p className="text-[15px] font-medium text-wallet-text">Label</p>
    <p className="truncate text-[13px] text-wallet-text-secondary">Subtitle</p>
  </div>
  {/* optional chevron */}
</Link>
```

## Placeholder / empty state
```tsx
<div className="flex min-h-[60vh] flex-col items-center justify-center gap-4 p-8 text-center">
  <img src={...} className="block size-36" alt="..." />
  <h2 className="text-lg font-semibold text-wallet-text">Title</h2>
  <p className="text-sm text-wallet-text-secondary">Description</p>
</div>
```

## Hooks rules
- Hooks only at the top of function components — never inside loops or conditions
- Use `useCallback` when referential stability matters, not mechanically for every handler
- Use `useMemo` for expensive derived values (not for trivial ones)
- Prefer `useSignal` from `@tma.js/sdk-react` over manual subscriptions to TMA events

## Dark mode
- The `dark` class is set on the root `<div>` by `App.tsx` based on `miniApp.isDark`
- Use `dark:` prefix sparingly — prefer wallet design tokens on product screens and TG tokens only on legacy demo screens
